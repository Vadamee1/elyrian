import { Cinzel_Decorative, Lacquer } from "next/font/google";

export const titleFont = Cinzel_Decorative({
  subsets: ["latin"],
  weight: ["400", "700", "900"],
});

export const ocultMessages = Lacquer({
  subsets: ["latin"],
  weight: ["400"],
});
